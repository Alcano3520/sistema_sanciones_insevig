name: sistema_sanciones_insevig
description: Sistema de Registro de Sanciones como el de Kivy - INSEVIG
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # Backend y base de datos
  supabase_flutter: ^2.3.4

  # UI básico
  cupertino_icons: ^1.0.2

  # Formularios (versión compatible)
  flutter_form_builder: ^10.1.0

  # Imágenes y firma
  image_picker: ^1.0.4
  signature: ^6.3.0
  
  image: ^4.1.7
  path_provider: ^2.1.2
  path: ^1.8.3

  # Estado
  provider: ^6.1.1

  # Utilidades (versión compatible)
  intl: ^0.20.2
  uuid: ^4.2.1
 
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true